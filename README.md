# lab01-jueves
Iniciamos creando las carpetas que usaremos que son web, api, 
para cambiar de nombre use un comando dv --->

Usando estos comando ya inicie y corri el docker: 
 docker build -t lab/api .
# docker run -p 3000:3000 lab/api