name: Saludando

on:
  push:
    branches:
      - main
      - develop

env:
  NOMBRE: "Darli Manuel"

jobs:
  saludo:
    runs-on: ubuntu-latest

    steps:
      - name: Saludo desde main
        if: github.ref == 'refs/heads/main'
        run: echo "Hola desde main, mi nombre es Harley Rabanal"

      - name: Saludo desde develop
        if: github.ref == 'refs/heads/develop'
        run: echo "Hola desde develop, mi nombre es Harley Rabanal"
